# Goal Objective

Generated by pi-goal on 2026-06-02T17:07:09.342Z.

Close out the multi-skill quality pass on the Pi fork (valkyriweb/pi-mono) and its extension repo (valkyriweb/my-pi), and resolve the remaining clawpatch findings — keeping all changes fork-owned and small, routing behavior customization through my-pi extensions/hooks/filters rather than fork core. Full source-of-truth requirements: docs/goals/goal-2026-06-02T17-06-37Z-acba13e7.md — read it before acting.

OUTCOME (what must be true when complete):
- Both repos located; each repo's local agent instructions read first (fork rules: fork-owned-only changes, no core behavior delta, commit hygiene, PR review gate, changelog rules).
- LIVE state re-verified, not trusted from the snapshot: fork main + open PRs (#32/#33/#34) and CI status confirmed; whether my-pi is still hot (active sessions, feat/pi-memory-merge-recall-ux progress, dirty tree) checked via intercom/git.
- Clawpatch reports in both repos inspected; each remaining finding judged still-real vs stale vs already-handled vs better-routed, with hidden risks and stop conditions called out.
- A clear review + recommendation delivered first: which findings to act on, which to drop, and why — covering all ~55 findings (33 fork RECORDED-not-patched + 22 my-pi).
- The my-pi findings decision recommended and, ONLY with the owner's explicit go, executed — choosing among: leave to the owning hot-branch session, land safe isolated ones (e.g. pi-routine logs-crash guard) via an isolated worktree, or wait until my-pi is quiet. The dream-memory recall-budget HIGH bug (200ms TOTAL_WALL_BUDGET_MS killing the 8000ms USER_PROMPT_SEARCH_MS recall) must be checked against feat/pi-memory-merge-recall-ux before touching anything.
- Optionally implement the two fork-owned follow-up bugs only if review supports it: (a) check-changelog-updated.mjs requiredChangelog returns the package changelog before its isTestOnly() check, wrongly requiring CHANGELOG for package test-only changes (likely a simple reorder); (b) tests pinned to live provider models breaking on provider drops — derive a valid model from the registry or pin the generated model set.

VERIFICATION EVIDENCE (required before completion):
- Any fork code change: the repo's `npm run check` passes with FULL output (no tail); every touched test file run individually and passing. Workflow/docs/test-only changes do not need a package changelog, but other code changes do per fork rules.
- my-pi changes: the relevant extension's tests/typecheck pass, plus the extension gate where applicable.
- Any clawpatch re-run uses the provider-qualified model claude-bridge/claude-opus-4-8 (NOT bare claude-opus-4-8, which misroutes to a capped OAuth route returning 400 "out of extra usage").
- Report the exact proof commands run and their results.

CONSTRAINTS:
- Do not refactor upstream-native files; do not add Effect to fork core (Effect stays in my-pi around flaky vendor/Promise boundaries only). No core behavior delta.
- Do not inject changes into another session's active my-pi branch; do not discard others' dirty changes or unrelated work.
- Do not push, merge, close issues/PRs, label, or post public comments unless explicitly told. Commit only your own files with explicit paths; never `git add -A` / `git add .`.
- No unapproved shortcuts, compatibility shims, TODO placeholders, dead code, duplicated logic, hidden assumptions, or undocumented behavior changes. Keep edits scoped and small.

ITERATION POLICY:
- After each attempt, inspect evidence, update the plan, take the next low-risk useful step; do not stop at a plan when implementation or verification remains.
- If validation fails, triage and fix the root cause rather than reporting partial completion.
- For long-running commands, CI runs, or external events, use monitor_start or an existing routine/wake mechanism.

BLOCKED STOP CONDITION:
- The owner's go is required before executing the my-pi findings decision. If that approval, or any access/tool/decision, is missing, stop without marking complete; report attempted paths, evidence gathered, exact blockers, remaining unmet requirements, and what input would unblock progress. Do not wait silently.

COMPLETION AUDIT:
- Before marking complete, map every explicit requirement above to fresh evidence (files, commands, diffs, tests, CI/logs). The goal is not complete if any requirement is unverified, narrowed, deferred, or only probably satisfied, or if the owner's decision on the my-pi findings is still pending. "Good enough"/"out of scope" are not valid completion evidence unless the original task allowed it.
