# Goal Objective

Generated by pi-goal on 2026-06-02T17:06:37.520Z.

I want to discuss and possibly continue/close out: a multi-skill quality pass on the Pi fork (valkyriweb/pi-mono) and its  
 extension repo (valkyriweb/my-pi), and decide how to handle the remaining clawpatch findings.                                
                                                                                                                              
   Context:                                                                                                                   
   - valkyriweb/pi-mono is a fork of earendil-works/pi-mono (upstream). Divergence is intentional, but the working rule is    
 fork-owned-only changes: do not modify upstream-native files (it inflates rebase/merge surface), and no core behavior delta  
 — customize via my-pi extensions/hooks/filters instead.                                                                      
   - A prior session ran these skills across the fork: code-craft, code-review/autoreview, agent-native-hardening (Matt       
 Pocock "improve codebase architecture" lens), effect-incremental-adoption, project-sdlc, and clawpatch. It implemented the   
 in-scope fixes and applied clawpatch to both repos.                                                                          
   - Merged to the fork's main (squash PRs): a governance/SDLC set, a github-copilot live-model drift fix (gpt-4o ->          
 gpt-5.5), and an agent-view module load-error-surfacing fix. See PRs github.com/valkyriweb/pi-mono/pull/32, /33, /34.        
 Re-verify main + CI; these landed but state may have moved.                                                                  
   - clawpatch gotcha (important): clawpatch's "pi" provider must use a PROVIDER-QUALIFIED model id,                          
 claude-bridge/claude-opus-4-8, NOT bare claude-opus-4-8. Bare resolves to pi's built-in Anthropic provider (a capped OAuth   
 route that returns 400 "out of extra usage") — this looks like billing exhaustion but is actually a misroute. Both repos'    
 clawpatch config were corrected to the qualified id. The codex provider is unreliable for clawpatch.                         
   - clawpatch produced ~55 findings total (33 fork + 22 my-pi), saved in each repo's clawpatch state/reports directory.      
                                                                                                                              
   Already decided / current state:                                                                                           
   - Fork findings deliberately RECORDED, not patched (owner's call: large merge footprint / upstream-origin / critical       
 path): OAuth credentials written world/group-readable and to a working-directory-relative auth.json (in the pi-ai CLI's      
 saveAuth + AUTH_FILE — those lines are upstream-native, so an upstream PR is the clean home); release/install hardening in   
 the build-binaries release workflow and the install-mini-pi install script (suppressed stderr + unconditional fallback; no   
 checksum/integrity manifest; non-atomic extraction); and a missing-test gap on the package-manager CLI argument parser.      
   - my-pi findings (22, all owned code) were produced but NOT implemented, because my-pi was hot (an active session on       
 branch feat/pi-memory-merge-recall-ux with many dirty files). They were routed to the owning session; the durable record is  
 the latest clawpatch report in my-pi's clawpatch state dir.                                                                  
   - One my-pi HIGH finding was validated as a REAL bug: in the dream-memory extension's harness CLI, the top-level           
 dispatcher wraps every hook handler in a 200ms wall budget (constant TOTAL_WALL_BUDGET_MS) while the UserPromptSubmit recall 
 search is given an 8000ms budget (constant USER_PROMPT_SEARCH_MS). The 200ms wall always fires first, so UserPromptSubmit    
 memory recall is effectively dead for any real query (the code comment itself notes qmd + LLM expansion takes 1-6s), and it  
 orphans the search because the timeout helper does not cancel underlying work. Other my-pi findings: the pi-routine logs CLI 
 crashes on a single malformed JSON line in its fire-log; fire-and-forget append/telemetry promises lack .catch               
 (unhandled-rejection risk); and an agentDir/rootDir divergence between the pi-routine logs and doctor commands.              
   - Effect verdict from this pass: keep Effect in my-pi (around flaky vendor/Promise boundaries), NOT in fork core. No       
 speculative rewrites.                                                                                                        
                                                                                                                              
   The open decision (the only thing blocking goal completion):                                                               
   - How to handle the my-pi clawpatch fixes: leave them to the session that owns the hot recall branch, land the safe        
 isolated ones (e.g. the logs-crash guard) via an isolated worktree, or wait until my-pi is quiet. The recall-budget HIGH bug 
 overlaps the feat/pi-memory-merge-recall-ux work — check whether that branch already addresses it before touching anything.  
                                                                                                                              
   Two follow-up bugs flagged but not actioned (fork-owned):                                                                  
   - The changelog-enforcement check (script check-changelog-updated.mjs, its requiredChangelog logic) returns the package    
 changelog for any packages/<pkg>/ path BEFORE its isTestOnly() check, so package-level test-only changes wrongly require a   
 CHANGELOG, contradicting its own error message and docs. Likely a simple reorder.                                            
   - Tests pin to live provider models, so they break whenever a provider drops one (this caused the gpt-4o -> gpt-5.5        
 churn). Consider deriving a valid model from the registry or pinning the generated model set.                                
                                                                                                                              
   Before doing any implementation:                                                                                           
   - Find both repos from the current directory, a parent directory, or the usual workspace.                                  
   - Read each repo's local agent instructions first (the fork has strict rules: fork-owned-only changes, no core behavior    
 delta, commit hygiene, a PR review gate, and changelog rules).                                                               
   - Re-check LIVE state: the fork's main and open PRs, CI status, and whether my-pi is still hot (active sessions, that      
 branch's progress, dirty tree). Sessions are ephemeral and shift — verify, do not trust this snapshot.                       
   - Inspect the clawpatch reports in each repo and judge which findings are still real, in-scope, and worth acting on.       
   - Decide whether each remaining item is a good idea, stale, already handled by the active my-pi work, or better routed     
 differently. Call out hidden risks and anything that should stop the work.                                                   
                                                                                                                              
   Task (only if your review supports it):                                                                                    
   - Recommend, and with the owner's go execute, the my-pi findings decision.                                                 
   - Optionally implement the two fork-owned follow-up bugs.                                                                  
   - Keep changes fork-owned and small; route any behavior customization through my-pi, not fork core. Use                    
 claude-bridge/claude-opus-4-8 for any clawpatch runs.                                                                        
                                                                                                                              
   Non-goals:                                                                                                                 
   - Do not refactor upstream-native files. Do not add Effect to fork core. Do not inject changes into another session's      
 active my-pi branch.                                                                                                         
                                                                                                                              
   Validation:                                                                                                                
   - Any fork code change: the repo's `npm run check` must pass with full output (no tail), and run any touched test file     
 individually. Workflow/docs/test-only changes do not need a package changelog.                                               
   - my-pi changes: the relevant extension's tests/typecheck, plus the extension gate where applicable.                       
   - Re-running clawpatch must use the provider-qualified model.                                                              
                                                                                                                              
   Output:                                                                                                                    
   - Start with your review findings and recommendation (which findings to act on, which to drop, and why).                   
   - Then the proposed plan or patch summary.                                                                                 
   - Keep edits scoped; report the exact proof you ran.                                                                       
   - Do not push, merge, close issues/PRs, label, or post public comments unless explicitly told. Commit only your own files  
 with explicit paths; never `git add -A`.
