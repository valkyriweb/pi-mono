# Goal Objective

Generated by pi-goal on 2026-05-14T08:42:25.952Z.

# Goal: finish autoresearch WIP — bash bg/output/kill + subagents config                                    
                                                                                                               
   Read `~/Projects/personal/pi-mono-fork/GOAL-finish-autoresearch-wip.md` fully before starting. It has the   
 full deliverables, drop list, watch-outs, and done-criteria.                                                  
                                                                                                               
   ## Starting state                                                                                           
   - Repo: ~/Projects/personal/pi-mono-fork                                                                    
   - Branch: feat/compaction-trigger-tokens (off main, contains d5ad4a51 — already PR'd as #5 into             
 valkyriweb/pi-mono main; do NOT touch it).                                                                    
   - Source branch with WIP: autoresearch/codex-cache-hit-rate-2026-05-12                                      
     - 5e3a4e3d — bash run_in_background + bash_output + bash_kill (likely cherry-pickable as-is)              
     - 81f12007 — wip snapshot with subagents slice + lots of unrelated churn (hand-port subagents only)       
                                                                                                               
   ## Order of operations (strict)                                                                             
                                                                                                               
   1. `git checkout feat/compaction-trigger-tokens && git pull` (no-op if local).                              
   2. **Bash bg first.** Try `git cherry-pick 5e3a4e3d`. If clean → typecheck + run touched tests + commit     
 message stays. If conflicts in `agents/executor.ts` (likely — recent forkAgent/running-tasks work on main:    
 7a803bcb, 76e16971), resolve by keeping main's structure and layering bg lifecycle on top. Add a teardown     
 test that confirms bg processes don't leak across session shutdown.                                           
   3. **Subagents second.** Hand-port from 81f12007 — only these slices:                                       
      - settings-manager.ts: SubagentThinkingSetting, SubagentDefaultSettings, SubagentSettings,               
 settings.subagents, getSubagentSettings()                                                                     
      - core/tools/agent.ts + core/agents/executor.ts + core/agents/types.ts + status.ts: precedence chain     
 (explicit task option > agent frontmatter > settings.subagents.providers[parent.provider] >                   
 settings.subagents.defaults > parent inheritance)                                                             
      - docs/settings.md: subagents section from the WIP diff is good, reuse                                   
      - tests: agent-model-selection.test.ts, additions to tools.test.ts and args.test.ts                      
      Write one unit test per precedence layer.                                                                
   4. **Do NOT port** from 81f12007: codex prompt cache affinity, generated model lists refresh, semantic-grep 
 sqlite blobs, .pi/ artefact churn, FORK-CHANGELOG noise. If a hunk is unclear → leave it out and note it.     
                                                                                                               
   ## Per-commit discipline                                                                                    
   - One feature per commit. `feat(bash): run_in_background + bash_output + bash_kill`, `feat(subagents):      
 default model/thinking config`.                                                                               
   - After each commit: `npx tsgo -p tsconfig.build.json --noEmit` (clean) + `npx vitest run` (green) in       
 packages/coding-agent.                                                                                        
   - If a single commit grows past ~10 files, split it.                                                        
                                                                                                               
   ## Full gate before declaring done                                                                          
   1. `cd packages/coding-agent && npx tsgo -p tsconfig.build.json --noEmit`                                   
   2. `cd packages/coding-agent && npx vitest run`                                                             
   3. `~/Projects/personal/rusty/scripts/update-pi-agent` to rebuild + reinstall the local pi binary           
   4. Smoke: `pi -p "reply ok" --model openai-codex/gpt-5.5` → returns "ok"                                    
   5. `FORK-CHANGELOG.md` updated with three bullets (triggerTokens already shipped, bash bg, subagents).      
                                                                                                               
   ## Done when                                                                                                
   - `git log main..feat/compaction-trigger-tokens` shows exactly three clean commits: d5ad4a51                
 (triggerTokens), bash bg, subagents.                                                                          
   - Full gate green.                                                                                          
   - Local pi rebuilt + smoke-tested.                                                                          
   - Report back with the three commit SHAs and `git diff main..HEAD --stat`. Do NOT open a PR — Luke will     
 decide when to ship.                                                                                          
                                                                                                               
   ## Watch-outs                                                                                               
   - `agents/executor.ts` is hot — recent main commits 7a803bcb (forkAgent silent:true) and 76e16971           
 (running-tasks panel + agent tool taskId wiring) touch it. Rebase early; expect conflicts there. Keep main's  
 structure and layer subagents resolution / bg lifecycle on top.                                               
   - The triggerTokens WIP commit was a no-op because callers didn't pass contextWindow. Same trap applies for 
 subagents: search for every read site of settingsManager and confirm new fields are actually consumed         
 end-to-end. Don't trust the WIP diff alone.                                                                   
   - Bash bg processes must be cleaned up on `/clear` and session reload — write the teardown test before the  
 implementation.                                                                                               
                                                                                                               
   ## Tier                                                                                                     
   Important. Batch related diffs per commit, show me the diff stats before pushing anything.
