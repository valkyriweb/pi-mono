# Scenario capture: subagents-s04-saved-workflow
# Prompt: define/run tiny saved chain or closest extension equivalent
# 2026-05-02TmanualZ

`pi-subagents` has explicit saved/reusable workflow surfaces.
Evidence:
- `~/.pi/agent/git/github.com/nicobailon/pi-subagents/src/slash/slash-commands.ts` registers `/run-chain`.
- `~/.pi/agent/git/github.com/nicobailon/pi-subagents/src/extension/schemas.ts` includes `chainName` and management `action`/`config` fields.
- `CHANGELOG.md` `0.19.1` added `/run-chain`.
- README command table documents `/run-chain <chainName> -- <task>`.

Result: stronger saved/reusable workflow ergonomics than native-only prompt snippets.
