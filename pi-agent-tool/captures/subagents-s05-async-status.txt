# Scenario capture: subagents-s05-async-status
# Prompt: /run scout --bg ... then /subagents-status
# 2026-05-02TmanualZ

`pi-subagents` has explicit async/background and status surfaces in source/docs.
Evidence:
- `~/.pi/agent/git/github.com/nicobailon/pi-subagents/src/slash/slash-commands.ts` parses `--bg` for `/run`, `/chain`, `/parallel`, and `/run-chain` via `extractExecutionFlags`.
- Same file registers `/subagents-status`.
- `~/.pi/agent/git/github.com/nicobailon/pi-subagents/src/tui/subagents-status.ts` implements the status UI component.
- `CHANGELOG.md` `0.19.0` mentions `/subagents-status` detail view.

Result: source-backed support; interactive background run capture still pending if needed.
