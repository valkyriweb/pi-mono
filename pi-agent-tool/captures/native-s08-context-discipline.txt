# Scenario capture: native-s08-context-discipline
# Prompt: child answers using only eval-design-prompt.md and eval-plan.md; compare context controls
# 2026-05-02TmanualZ

Native Pi exposes explicit context discipline controls on the built-in `agent` tool.
Evidence:
- `packages/coding-agent/src/core/tools/agent.ts` schema includes `context` with modes such as `default`, `fork`, `slim`, and `none`.
- `packages/coding-agent/src/core/agents/context.ts` implements `resolveContextPolicy()` and filters forked context, including stripping native `agent` and legacy `subagent` artifacts.
- `packages/coding-agent/test/agent-context-inheritance.test.ts` verifies context modes and artifact filtering.
- `packages/coding-agent/docs/usage.md` documents context modes.

Result: native has strong, explicit context-discipline controls suitable for fair low-token child tasks.
