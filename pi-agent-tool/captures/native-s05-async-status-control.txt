# S05 native async/background/status/control
Evidence mode: source-backed plus tmux `/agents-status` capture plus unit-test audit plus paid live background start/status, interrupt/resume, and cancel probes.
Native now exposes `background: true` plus generic background-run status/detail/interrupt/cancel/resume control through the `agent` tool and `/agents-status` hints. This is source-backed, covered by the cheap status capture, audited against existing status/control unit tests, validated by `captures/native-s05-background-control-live.txt` for a real background child start, run id, `/agents-status <run-id>` detail, child README read tool, output, and cost, validated by `captures/native-s05-background-interrupt-resume-live.txt` for interrupt, resumable status, resume, completed output, and cost, and validated by `captures/native-s05-background-cancel-live.txt` for a paid cancel probe with cancelled status, no final child output, and cost.
