# @valkyriweb/pi-coding-agent

This package's release notes are split:

- **Fork-specific notes** (the canonical source) live in the repo-root
  [`FORK-CHANGELOG.md`](../../FORK-CHANGELOG.md).
- **Upstream history** (earendil-works/pi-mono, Keep-a-Changelog format) is
  archived in [`CHANGELOG.upstream.md`](./CHANGELOG.upstream.md).

## Unreleased

- Bash policy: ban process substitution `<(...)` / `>(...)` in the read-only `detectUnsafeConstructs` scanner (`EXPLORE_BASH_POLICY`, applied to the `explore` child agent). It previously rejected command substitution `$(...)`, backticks, and here-docs but not process substitution, so `cat <(rm -rf x)` / `tee >(rm -rf x)` could run an arbitrary inner command while presenting as a benign `cat`/`tee`. Adds a `/[<>]\(/` check plus a regression test (`test/bash-policy.test.ts`). Found via Claude Code 2.1.207–2.1.212 reverse-engineering triage (my-pi #1089).

- Force-exit one-shot `--print`/`--mode json` runs after completion so a leaked event-loop handle (observability sockets, sidecar children, metric export timers) can no longer keep the process alive until the harness timeout, matching the existing package-command one-shot guarantee. win32 drains naturally to avoid an assert when `process.exit()` follows `fetch()` during teardown (nodejs/node#56645) ([my-pi#1080](https://github.com/valkyriweb/my-pi/issues/1080)).

- Removed the always-added generic "Be concise in your responses" system-prompt guideline; it contradicts mandated structured handoff summaries on compression-biased models (context audit C1, [#322](https://github.com/valkyriweb/pi-mono/pull/322)). Scoped brevity policy lives in operator instruction files.

- Unified child-agent preparation across initial dispatch and persistent resume so model/thinking defaults, auto-routed model choices, output caps, tool/depth policy, cwd-bound services, and per-run `maxTurns` remain consistent across turns ([#277](https://github.com/valkyriweb/pi-mono/issues/277)).

- Fixed extension `ctx.isIdle()` reporting true during compaction and turn-start lifecycle work, including prompt preflight and resumed interactive tools, so idle-only shortcuts and commands no longer queue work while Pi is busy ([#295](https://github.com/valkyriweb/pi-mono/issues/295)).

- Accept model-registry compatibility flags for opaque Codex gateways to omit ChatGPT account headers, force SSE, and disable zstd request compression.

- Fix Codex tool-name filtering to follow `openai-codex-responses` API selection, including gateway providers, instead of the direct `openai-codex` provider id.

- Fixed custom `agent`/`Agent`/`Task` aliases failing with `agent tool is unavailable in this runtime` after the session-isolated Agent engine change. Agent tool definitions now fall back to the calling session's execution-scoped engine when an alias has no explicit/runtime engine, preserving extension overrides and profile/depth denial.

- Forked named Agent profiles now retain their role prompt as trailing task guidance while preserving the caller's system/tool cache prefix. Fork remains a permissive self-fork: callers receive a warning when ordinary profile tool filtering is bypassed and can use `context: "default"` when that filtering matters; existing profile/depth caps on nested Agent access remain enforced.

- Agent task prompts now address the calling agent rather than assigning child/subagent identity, with concise outcome, evidence, and verification contracts plus accurate depth-aware Agent availability on initial and resumed turns. Public tool call shapes remain compatible and fork task guidance stays in trailing user messages for cache preservation.

- Extensions: add a dedicated `custom_message` event for every custom message accepted by `AgentSession.sendCustomMessage()`, including idle background completions that intentionally bypass the agent-loop `message_start` / `message_end` lifecycle. Idle wake scheduling now waits for these observers so completion consumers can finalize durable side effects before the parent continues.

- Fixed: resuming a parked persistent background Agent run (`resumeSingleBackgroundRun`) rebound the child's tool registry (bash/read/write/etc.) to the parent's cwd instead of re-applying the routed `task.cwd`, so a background dispatch's first resumed turn ran outside its intended worktree/dir (valkyriweb/my-pi#916). The initial dispatch already resolved this correctly via `resolveChildCwd`; the resume path now mirrors it.
- GPT child agents prefer GPT-5.6 variants during model resolution (fork PR #258)
- Retired GPT-5.4/5.5 from automatic child-agent tiers and provider defaults; OpenAI/Azure and ClawRouter use GPT-5.6, while direct Codex and Copilot use current catalog-backed 5.3 fallbacks.
- Explore child agents now run an isolated read-only tool profile (fork PR #254)
- Session crash tombstones (browser-style restore): stale `.live` liveness markers left by a dirty shutdown (crash, SIGKILL, power loss) are preserved as `<session>.jsonl.crashed` tombstones instead of being deleted by the resume picker and the startup sweep. The resume picker badges crashed sessions with a red `✗` (parallel to the green `●` live badge), opening a session clears its tombstone, and the sweep ages out tombstones older than 30 days. Graceful exits — including SIGTERM/SIGHUP from closing a terminal tab — still remove the marker and are never marked crashed. New `listCrashedSessionPaths()` export for resume UIs. Also fixes the vacuously-passing live-badge picker test broken by the Named-filter default (#166): unnamed fixtures were filtered out of the list entirely.

- Footer: include dynamic extension-registered pill output in the whole-footer render memo key and make `FooterComponent.invalidate()` evict memoized lines, so monitor, workflow, agent, and background-task pills repaint as their state changes. Regression in `test/footer-width.test.ts` fails on the prior implementation.

- Auto-compaction failure circuit breaker: transient provider-availability errors (429 rate-limit/usage-limit windows — e.g. ChatGPT-Codex `usage_limit_reached`, which carries `resets_in_seconds` — 529/503 overload shedding, gateway errors) no longer count toward the 3-strike trip and no longer reset a real-failure streak; when an attempt was already announced, a distinct `compaction_end` message marks it as retryable instead of a breaker strike. Previously a usage-limit window tripped the breaker and permanently disabled auto-compaction for a session that resumes working once the limit resets, leaving it to grow unchecked until the context-window limit (observed 2026-07-10: two sessions died this way). New pure helper `isTransientCompactionError` in `core/compaction/utils.ts`; regressions in `test/autocompact-thrashing-detector.test.ts` and `test/compaction.test.ts` (baseline-fail verified).

- Add `max` thinking level (GPT-5.6+) across CLI `--thinking`, settings, thinking/settings selectors, agent tool schema, and theme (reuses the `xhigh` color).
- Add model-gated `ultra` across the same surfaces, described as maximum reasoning plus a client-side orchestration signal and rendered with the `xhigh` color.
- Fix startup freezes on large session trees by traversing the deferred stale-session-marker sweep asynchronously while preserving live, dead, and stale marker semantics.

- Refactor (fork-delta reforge slice 1, no behavior change): extracted the fork's resident session pruning subsystem (~590 lines — in-memory prune, load-time raw-metadata prune plan, payload byte estimation, entry stubbing, streaming JSONL line reader) from `core/session-manager.ts` into fork-owned `core/session-resident-prune.ts`. `SessionManager.pruneResidentHistoryAfterCompaction` delegates to the new pure `pruneResidentHistory(entries, path, compactionId, options)`; moved public symbols (`estimateResidentPayloadBytes`, `ResidentPruneOptions`, `ResidentPruneResult`) are re-exported from `session-manager.ts` so downstream imports are unchanged. Verbatim move; existing `test/session-manager/resident-prune.test.ts` and compaction suites pass unmodified.

- Refactor (fork-delta reforge slice 2, no behavior change): decomposed the fork's additions to `core/tools/bash.ts` (2013 → ~770 lines) into fork-owned modules — `core/bash-bg-jobs.ts` (background job registry/lifecycle, spawn/adopt, timeout-disposition seam, concurrency ceiling, stale-log sweep, log reading), `core/bash-policy.ts` (read-only child-agent bash policy, native-tool steering guard, redundant-cd guard, semantic exit-code classification), `core/bash-script-segmenter.ts` (embedded-script detection for call rendering), `core/tools/bash-output.ts` and `core/tools/bash-kill.ts` (bash_output/bash_kill tool definitions). All moved public symbols are re-exported from `tools/bash.ts` so downstream imports (`src/index.ts`, `core/tools/index.ts`, tasks, extensions, tests) are unchanged. Verbatim moves; tool descriptions, schemas, and prompt guidelines are byte-identical. Existing bash/tasks suites pass unmodified.

- Refactor (fork-delta reforge slice 3, no behavior change): extracted the fork's additions to the extension system out of the three upstream-modified files. `core/extensions/types.ts` (+966 → +661 vs upstream): B5 TUI slot types moved to fork-owned `ui-slots.ts` (now also owns `ExtensionSlotUIActions`, previously in runner.ts) and the `ctx.forkAgent`/`ctx.transcript` API types (`AgentHandle`, `ForkAgentOptions`, `ForkAgentResult`, `TranscriptApi`, `TranscriptEntry`) to `fork-agent-types.ts`, both re-exported from `types.ts`. `core/extensions/runner.ts` (+527 → +410): registry walkers (session-dispose fan-out, registered agent definitions/chains/context-modes, B5 main-pane/overlay/footer lookups, default message renderers) moved to `runner-registries.ts` (all synchronous — no scheduling change); the deferred-extension batch loader body to `deferred-loading.ts` (the runner method returns the batch promise directly, keeping the await chain tick-identical). The cache-critical `message:end` / `provider:beforeRequest` / `systemPrompt:build` filter seams deliberately stay inline: extracting them into async wrapper functions adds promise-adoption microtask ticks on the prompt hot path, which observably changed scheduling (caught by `test/suite/regressions/5943-session-start-notify.test.ts` — a fire-and-forget `session_start` `sendUserMessage` lost its race with `waitForIdle`). `core/extensions/loader.ts` (+508 → +194): the entire fork-added `ExtensionAPI` slice (`setDefaultMessageRenderer`, `onSessionDispose`, live-session registry trio, `registerAgentDefinitions`/`registerAgentChains`/`registerContextMode`, run-registry/telemetry accessors, B5 register/show/hide/footer methods, `getExtensionConfig`, `tools`/`hooks`/`harness` namespaces, `state()`, `service()`/`getService()`) plus the process-service map and `createHookHandle` moved to `extension-api-fork.ts`; `createExtensionAPI` spreads `createForkExtensionAPI(extension, runtime)` into the API literal, and `getExtensionProcessService`/`deleteExtensionProcessServiceForTests` are re-exported from `loader.ts` so downstream imports (agent-session.ts, tests) are unchanged. Verbatim moves with identical runtime wiring; existing extension/runner/hook suites pass unmodified.

- Refactor (fork-delta reforge slice 4, no behavior change): extracted the fork's interactive-mode UI additions into fork-owned modules. `modes/interactive/interactive-mode.ts` (+780 → +596 vs upstream): the `/agents` and `/agents-doctor` command family (subcommand parsing, agents/runs selectors, run-row zoom/resume/interrupt/cancel actions, doctor report — five private methods) moved to `modes/interactive/agent-commands.ts` (`runAgentsCommand`/`runAgentsDoctorCommand`) behind a narrow `AgentCommandsHost` seam; `InteractiveMode` builds the host adapter per invocation so private UI members stay private and `session` is never captured stale. `modes/interactive/components/footer.ts` (+517 → +313): the memoized usage-totals scan (`UsageSnapshot`/`UsageTotals`, cache-key composition, post-compaction and follows-user-turn flags) moved to `components/footer-usage.ts` (`FooterUsageTracker.getTotals(entries)`); `FooterComponent.getUsageTotals()` delegates. Footer-nav and main-pane/overlay state handling deliberately stay inline (mutable UI state interleaved with upstream escape logic on the input hot path). Verbatim moves; agents/footer/interactive-mode suites pass unmodified.

- Refactor (fork-delta reforge slice 5, no behavior change): extracted the fork's prompt-cache heartbeat subsystem from `core/agent-session.ts` (+1974 → +1751 vs upstream) into fork-owned `core/cache-heartbeat.ts`. `CacheHeartbeatManager` owns the base (process-global, static-prefix) and per-session heartbeats — working-hours gating, provider allowlist, global per-model rate-limit cooldown and base-warmth bookkeeping, timers, and the opportunistic minimal-token refresh request — behind a `CacheHeartbeatHost` interface whose members read live session state (getters/closures, never snapshots). `AgentSession` keeps three seams: `setSessionTarget`/`noteActivity` on the prompt path and `dispose()`. Method bodies moved verbatim (only `this.*` → `this.host.*` receiver renames); heartbeat request bytes (systemPrompt, converted messages, tools, sessionId, cache affinity key) are unchanged. Heartbeat characterization tests in `test/suite/agent-session-compaction.test.ts` now target the manager's internals. Slice 5b additionally moved the model-facing tool-result hygiene helpers (`capModelFacingToolResultText`, `replaceUnsupportedToolResultImages` and their constants/helpers) verbatim into fork-owned `core/tool-artifacts.ts` (+1751 → +1541 vs upstream); `afterToolCall` call sites unchanged.

- Cache health: classify Anthropic thinking-block strips as `thinking_strip_likely` instead of `cache_write_unhealthy`, rendered as a dim `⟳think` footer marker (analogous to `⟳compact`). When a non-tool-result user message follows an agentic loop, the Anthropic API strips the loop's accrued thinking blocks from history, collapsing the warm prefix to the tools+system anchor — an expected one-time rewrite, not prefix drift (diagnosed from session `019f40ab` turn 70: 140,605→26,913 read collapse, 121,790 write, 2m46s gap). Detection: new `followsUserTurn` signal (user-role entries between assistant turns) + warm→anchor collapse (< 50% of ≥ 30k previous read), > 5k write, gap under the TTL window, same model. Regressions in `test/cache-health.test.ts` (baseline-fail verified).

- Agents runs selector: resume now prompts for an optional steering message before reattaching a resumable run, matching the existing Agent control `resume(message)` path while keeping empty-Enter as an explicit resume-without-message action.

- Extension main panes can now opt out of taking keyboard focus with `captureInput:false`, allowing display-only panes such as agent zoom to keep editor-driven input for text injection, `/exit-zoom`, and editor-scoped hotkeys while interactive panes continue to receive focus. Regression in `test/interactive-mode-footer-nav.test.ts`.

- Footer: remove implementation adapter badges such as `adapter:codex` from the model line; adapter status now belongs to extension footer/status UI, keeping provider/model/thinking display uncluttered. Regression in `test/footer-width.test.ts`.

- Cache health: classify Anthropic thinking-block strips as `thinking_strip_likely` instead of `cache_write_unhealthy`, rendered as a dim `⟳think` footer marker (analogous to `⟳compact`). When a non-tool-result user message follows an agentic loop, the Anthropic API strips the loop's accrued thinking blocks from history, collapsing the warm prefix to the tools+system anchor — an expected one-time rewrite, not prefix drift (diagnosed from session `019f40ab` turn 70: 140,605→26,913 read collapse, 121,790 write, 2m46s gap). Detection: new `followsUserTurn` signal (user-role entries between assistant turns) + warm→anchor collapse (< 50% of ≥ 30k previous read), > 5k write, gap under the TTL window, same model. Regressions in `test/cache-health.test.ts` (baseline-fail verified).

- Agents runs selector: resume now prompts for an optional steering message before reattaching a resumable run, matching the existing Agent control `resume(message)` path while keeping empty-Enter as an explicit resume-without-message action.

- Footer/agent status: surface provider-side resolved auto models from assistant metadata (for example `clawrouter/auto → gpt-5.5`) and show model + thinking details in `/agents status`, background-agent footer status, and the agent runs selector. Regressions in `test/footer-width.test.ts`, `test/agent-status.test.ts`, and `test/agent-runs-selector.test.ts`.

- Keep Edit tool TUI output within terminal width: render Edit errors as wrapped text and clamp/wrap ColorDiff lines so long diagnostics, emoji, and markdown table rows cannot crash the interactive renderer with `Rendered line ... exceeds terminal width`.

- **Persistent-fork lifecycle primitive + `AgentHandle.resume`/`inject` seam (CC 2.1.201 observer-agent enablement, Slice E core).** Adds the minimal core primitive an out-of-tree extension needs to run a CC-faithful long-lived background observer (a background subagent fed digests turn-after-turn), which pi's fork machinery could not express because a completed background run was terminal (controller deleted, session retired). New optional `ForkAgentOptions.persistent?: boolean` (default unset = unchanged terminate-on-completion): when a single background fork is `persistent`, it now **parks as `"interrupted"` after each turn instead of `"completed"`** — the only non-running status that `status.ts` keeps the run controller alive for (`applyRunDetails` deletes the controller on completed/cancelled/failed only) and that `canResumeRun` accepts (interrupted + mode single + execution background + a persisted `sessionPath`). The launcher feeds the next turn with the new `AgentHandle.resume(message)` (reloads the disk-persisted child session via `resumeSingleBackgroundRun` → cross-turn history preserved) and can steer a mid-turn child with `AgentHandle.inject(message)` (session.steer; rejects when the fork is parked/idle with no active session); both were added to the `AgentHandle` interface in `core/extensions/types.ts` and implemented in `core/agents/engine.ts` over the existing `resumeAgentRecentRun`/`injectAgentRecentRun` control functions (throw on `!result.ok`). The park is implemented in `core/agents/executor.ts` via a single `isPersistentPark(input)` helper (`persistent === true && background === true && mode === "single"`) applied at both single-run completion sites; the subtle part is that the child completing emits a `"completed"` progress aggregate through `makeBackgroundOptions.onProgress` → `updateAgentRecentRunProgress`, which would mark the run terminal and delete its controller *before* the park runs — so for persistent runs that terminal `"completed"` progress is remapped to `"running"`, making the park's `finishAgentRecentRun(interrupted)` the sole terminal transition (controller retained, `resumable` true, waiters resolve on interrupted). `abort()` still retires a parked fork (cancel is valid on interrupted → `"cancelled"`). No `params.system`/`tools[]`/cache-key surface touched; non-persistent forks are byte-for-byte unaffected (the remap and park are gated behind `persistent === true`). Regressions: 4 new tests in `test/suite/extensions/fork-agent.test.ts` (park-as-resumable-interrupted, resume-preserves-history-in-same-session, inject-rejects-when-parked, abort-retires) — all fail on baseline `bfe01c60a` (park absent → run completes, controller deleted, resume throws "cannot resume in this process") and pass after; full `fork-agent.test.ts` (16), `test/suite/extensions` (21), and the executor/background-lifecycle suites (`agent-tool-single`, `agent-tool-parallel-chain`, `agent-tool-context-modes`, `nested-delegation-depth`, `structured-output-fork`, `regressions/agent-background-terminal-notify`, `regressions/1717-2113-agent-session-event-settlement`) green.

- **Auto-compaction thrashing detector (CC 2.1.201 parity, Slice C).** Three ordered circuit breakers added to `AgentSession._runAutoCompaction`, all gated behind new private fields (`_consecutiveRapidRefills`, `_consecutiveCompactionFailures`, `_autoCompactDisabledThisSession`) and pure helpers in `core/compaction/utils.ts` (`RAPID_REFILL_WINDOW`, `RAPID_REFILL_TRIP_COUNT`, `COMPACTION_FAILURE_TRIP_COUNT`, `evaluateRapidRefill`): (1) a failure circuit breaker checked before anything else — repeated compaction exceptions (`COMPACTION_FAILURE_TRIP_COUNT` in a row) permanently disable auto-compaction for the rest of the session via `_autoCompactDisabledThisSession`, short-circuiting every subsequent check with zero emitted events; (2) a fixed-prefix-overflow guard — if the immovable prefix (system prompt + tool schemas, estimated via the existing chars/4 heuristic, reusing `estimateSystemPromptTokens`) alone exceeds `contextWindow - reserveTokens`, compaction can never succeed no matter how much is cut, so it short-circuits with a distinct static error message instead of attempting-then-looping; scoped to the periodic `"threshold"` trigger only (not one-off `"overflow"` recovery, which is a reactive single attempt after the provider already hard-stopped and still deserves a real try); (3) a rapid-refill (thrashing) breaker — `evaluateRapidRefill` trips after `RAPID_REFILL_TRIP_COUNT` consecutive compactions where context refilled back to the trigger point within `RAPID_REFILL_WINDOW` assistant turns, reusing a new `_assistantTurnsAfterCompaction` helper factored out of the existing `_appendCacheHealthEntry` turn-counting logic. All three breaker messages are static string literals via the existing `compaction_end`/`errorMessage` channel — no interpolation, no `tools[]`/system-prompt/cache-key surface touched. Regressions: `test/compaction.test.ts` `evaluateRapidRefill` describe block (pure function, 3 tests) and new `test/autocompact-thrashing-detector.test.ts` (4 wiring tests exercising `_runAutoCompaction` directly with a fully offline in-memory session — failure breaker short-circuit before rapid-refill, real catch-block failure-counter trip across 3 real calls, fixed-prefix-overflow guard trip, rapid-refill trip + streak reset); all fail on baseline (`evaluateRapidRefill is not a function`, missing fields/events) and pass after. Fixed two pre-existing tests (`test/suite/agent-session-compaction.test.ts` "compacts mid-run...", `test/suite/regressions/pre-prompt-compaction-no-continue.test.ts") that used synthetic near-window `reserveTokens` purely to force compaction cheaply on tiny fake usage — with the real ~400-2000 token system-prompt+tools prefix, those synthetic thresholds legitimately tripped the new guard; adjusted their settings/payload sizes to keep realistic headroom above the fixed prefix rather than weakening the guard. `npm run test:build-gate` green (all legs); full `packages/coding-agent` suite has 4 pre-existing unrelated failures (`session-liveness.test.ts`, `agent-definitions.test.ts`, `agent-permissions.test.ts`, `agents-ui.test.ts`) confirmed present on unmodified baseline `6d014fcdd`.

- **Agent tool prompt-wording batch (CC 2.1.201 fork/subagent-prompting RE port, Slice B — items 1/2/3/5/7/8 of `PORT-SPEC-fork-subagent-prompting.md`).** Six small S-effort prose additions to `core/tools/agent.ts`'s `promptGuidelines` and schema `.describe()`/tool `description` strings, all pure string literals with zero per-call/per-session interpolation (no behavior change): (1) a new `context:"fork"` explainer paragraph in `promptGuidelines` (what fork mode inherits, why it's cheap, when to reach for it vs `default`/`slim`/`none`); (2) explicit parallel-cap guidance ("up to 8 tasks... minimum number of agents necessary, usually 1") next to the existing parallel-fan-out bullet, so the model learns the cap from prose instead of discovering `tasks[]`'s `maxItems: 8` via a schema validation error; (3) a "trust but verify" bullet — a child's summary describes intent, not proof, so check actual file changes before reporting writer/worker/general output done; (5) a cache-affinity clause appended to the tool's top-level `description` ("Forks share the parent's prompt cache..."); (7) a clarity-only addition to the `background` field's `.describe()` stating it defaults to `false`/synchronous (pi's opt-in default is unchanged — this only documents it, doesn't flip it to CC's opt-out default); (8) a caution appended to the existing `sessionPaths`/`outputPaths` bullet to skim/grep a large session transcript rather than reading it whole. Items 4 (worktree/cwd context-quarantine notice) and 6 (`skillsToPreload`) from the same spec are separate, larger slices and are intentionally not included here. No golden/snapshot test locks this exact wording (checked `test/system-prompt.test.ts`, `test/suite/agent-tool-context-modes.test.ts`, `test/deferred-tool-guidelines.test.ts`, and repo-wide for `.snap`/`toMatchSnapshot` hits on these strings — none found); `npm run test:build-gate` passes.

- **Footer render + background-agent status ticks are now memoized/guarded instead of unconditionally rebuilding on every call (footer render memo + progress-tick change guard).** `FooterComponent.render(width)` rebuilt the entire footer string set (theme.fg colored spans, padding, truncation) every invocation even when nothing displayed had changed; it now keys a memo on every input that can affect the output (width, cwd/branch/session, extension-footer text, usage/context totals, model/thinking/provider state) and returns the previously-rendered lines untouched when the key is unchanged. Regression: `test/footer-width.test.ts` ("memoizes rendered lines when nothing that affects the footer changed", fails on baseline — pre-fix returns a fresh array every call). Separately, `core/agents/status.ts`'s `updateAgentRecentRunProgress` (the background-agent progress-tick path that unconditionally called `notifyAgentRecentRunsChanged()` -> global `ui.requestRender()`) now compares a content signature (status/error/attention/tool-call/output-snippet state, excluding pure timestamp fields) before notifying, and skips the notify when a tick carries no user-visible change; real content changes and terminal transitions (finish/fail) still always notify. Regression: `test/agent-status.test.ts` ("skips notifying subscribers when a progress tick has no user-visible content change", fails on baseline). Render-side only — no change to `params.system`/`tools[]` or any behavior/output, pure caching layer.

- **Remove the native `Ls` tool from defaults and stop blocking bash `ls` (Claude Code 2.x / Codex parity).** CC 2.x dropped its `LS` tool (verified: no `LS`/`Ls` tool strings in the CC 2.1.201 binary) and Codex never had one — both list directories via shell `ls`. `Ls` is gone from `defaultActiveToolNames` (sdk.ts + agent-session.ts), the system-prompt fallback tool list, and the builtin subagent tool lists (explore/decompose/plan/reviewer, which keep `Glob` for layout discovery); `ls` is no longer in the bash native-tool guard's rejected set, so directory listing via Bash `ls` now runs. The guard keeps rejecting standalone `grep`/`rg`/`find` (deliberate CC Grep-steering parity). Core `ls.ts` and its factories stay (upstream-owned, still SDK-opt-in via `tools: ["ls"]`) — zero upstream-sync conflict surface. Fixes the false positive where a guarded name inside a quoted remote command (`kube-exec … -- sh -c '…; ls pipelines scripts'`) was blocked with steering to a native tool that cannot reach the remote host. Regression: `test/bash-native-tool-guard.test.ts` (ls cases flip to allowed incl. the quoted remote-exec repro; fails on baseline), `test/suite/regressions/cache-break-builtin-preservation.test.ts` (Ls no longer a default builtin).

- **Extension main panes (e.g. the footer agents-status pill's pane) never received keyboard focus once opened, so no keystroke ever reached their `handleInput` -- this is the actual root cause of "Enter doesn't work" on the agents footer pill.** `interactive-mode.ts`'s `showExtensionMainPane` mounted the pane component into `chatContainer` but, unlike every other editor-replacing surface in the file (extension selector, extension input, dialogs, session selector, ...), never called `ui.setFocus(component)` -- so all keystrokes kept routing to the (empty, focused) main editor. Only Escape worked, via the separate `onEscape` passthrough in `handleEscapeKey`. This made the zoom-routing fix directly below (and the pane's pre-existing up/down navigate + `x` stop) unreachable dead code in the live product even though it was correct and covered by unit tests -- the unit tests construct the pane directly and call `handleInput` themselves, bypassing the missing focus wiring entirely, which is why this was caught only by live tmux verification, not by the test suite. Fixed by adding the missing `ui.setFocus(component)` on show and `ui.setFocus(this.editor)` on hide (restoring focus, matching the same restore pattern used by every other such surface). Regression: `test/interactive-mode-footer-nav.test.ts` ("focuses the pane component when it is shown...", "restores focus to the editor when the pane is hidden", fail on baseline).

- **Agents footer pill: Enter on a running background agent row now attaches to its live transcript instead of only ever showing a static status-text detail.** `core/extensions/agents.ts`'s footer-activated `AgentsPane` (added in #196) reused `agent-runs-selector.ts`'s row/detail formatters but never wired the row-scoped zoom entry (`shouldZoomAgentRunRow` + the `pi-agent-ui` "zoom" main pane) that `/agents runs` already gets from `handleAgentRunSelectorAction` -- Enter on this pane's rows *always* fell to the static detail view regardless of task state, so a user selecting the pill and pressing Enter to "enter into the agent session" got a text dump instead, reported as "it doesn't work". Added a generic, additive `pi.hasMainPane(id)` / `pi.cwd` extension-API seam (existence check for a registered main pane + the extension's working directory, mirroring the existing `showMainPane`/`hideMainPane`) so core can safely probe whether a richer pane is available before routing into it, falling back to the pre-existing static detail when it is not (e.g. a lean profile without `pi-agent-ui` loaded). Regression: `test/agents-ui.test.ts` ("Enter zooms into the live transcript...", "Enter falls back to static detail...", fails on baseline -- previously always showed detail).

- **Agents footer pill: distinct colors for "needs attention" vs "still running", and the pending question text now shows inline on the row.** The pill previously rendered every state identically (plain text, or accent-only when the pill itself had nav focus) -- a background agent silently blocked awaiting input read no differently than one still working. `render()` now colors the pill `warning` when any relevant task is interrupted, failed, or a running local_agent has `needsAttention`, and `dim` otherwise. `agent-runs-selector.ts`'s `formatAgentRunRow` also now inlines the actual pending question (`run.attentionMessage`) instead of only the "needs attention" label, matching CC's row-level display. Regression: `test/agents-ui.test.ts` ("footer pill uses a distinct color..."), `test/agent-runs-selector.test.ts` ("row inlines the pending question text...").

- `pi agents` CLI: the standalone jiti loader's `createJiti` import is now from `"jiti/static"` instead of `"jiti"`. The dynamic `"jiti"` entry lazy-requires its babel transform via a relative path (`../dist/babel.cjs`) at call time, which does not exist inside the compiled Bun binary (only statically-declared imports get bundled), so every load attempt failed with `Cannot find module '../dist/babel.cjs'` — silently swallowed and reported as `requires the pi-agent-view package`. This is the third and final part of the `pi agents` "requires the pi-agent-view package" fix (see #205, and the `virtualModules`/`alias` sharing fix directly below) — with all three, `pi agents` now actually launches against a real installed bundle. Regression coverage extends `test/agent-view-command.test.ts`.

- `pi agents` CLI: the standalone jiti loader used only by this CLI dispatch path now shares the main extension loader's `virtualModules`/`alias` resolution config (`getExtensionJitiResolutionOptions()`), so it can actually load the agent-view module's own `@valkyriweb/pi-coding-agent`/`@valkyriweb/pi-tui` imports instead of only resolving the entrypoint file and then silently failing on those inner imports. This is the second half of the `pi agents` "requires the pi-agent-view package" fix (see #205 below) — the resolver alone found the package but loading it still failed. Regression coverage extends `test/agent-view-command.test.ts`.

- `pi agents` CLI: the dashboard-package resolver now also matches scoped `pi-agent-view` publishes (e.g. `@valkyriweb/pi-agent-view`), not only the bare unscoped name. Bundles that publish the extension under an npm org scope previously hit `Error: \`pi agents\` requires the pi-agent-view package` even though it was installed and enabled, because the resolver's exact-string match against enabled extensions' nearest `package.json` `name` never matched a scoped name. Regression in `test/agent-view-command.test.ts`.

- Keybindings: add `app.agentView.back` (default: `left`), a new additive `AppKeybindings` entry for consumer extensions that need a user-remappable "go back" binding (e.g. `my-pi`'s agent-view dashboard returning from an attached session). Purely additive — no existing binding, dispatch, or interactive-mode wiring is touched; consumption is entirely the extension's own responsibility via `KeybindingsManager.matches()`.

- Tasks: `TaskSnapshot` gains an additive `controlId?: string` field, set on `LocalAgentTask`'s per-child snapshots (the leaf rows under `children` for an Agent-tool batch) to the owning run's real id. A child's own `id` (e.g. `"agent-5:1"`) is a display-only label minted per sub-run — there is no independent adapter, live session, or message buffer registered under it, only under the top-level run's id — so a consumer that flattens `children` into rows (e.g. an extension's task panel) and threads a child's bare `id` into a control/zoom operation gets a silent "not found" even though the run is live and controllable. `controlId` gives such consumers the id that IS registered. Regression in `test/tasks-registry.test.ts`.

- Agents footer pill: `render()` now honors the pill-nav `selected` context and highlights (accent color) when the agents-status pill has focus. Previously `render()` ignored its `ctx` argument entirely, so Down/pill-focus cycling never produced any visible highlight for this pill even though the underlying pill-nav selection state machine tracked focus correctly (`onActivate`/`showMainPane` worked; only the visual indicator was missing). Regression in `test/agents-ui.test.ts`.

- Agents: split the "agents" load action into `agentsTools` (native `agent`/`Agent`/`Task` tool schemas) and `agentsUI` (agents-status footer pill + main pane), registered as independent load actions. A consumer overriding the native tool schemas (e.g. to avoid duplicate/conflicting definitions) previously had to remove the entire bundled action to do so, which silently deleted the interactive footer pill and pane with no equivalent replacement — the reported "footer pill nav dead" defect. Removing only `agentsTools` now leaves the interactive surface intact. `hookAgents` remains available as a combined convenience wrapper. Regression in `test/agents-ui.test.ts`.

- Agents pane: the static background-agents pane now has real keyboard controls — up/down row selection, Enter to open the selected run's detail, and `x` to stop the selected running task — reusing the `/agents runs` selector row formatting instead of a read-only list.

- Footer pills: pressing up past the first pill (or up with nothing selected) now deselects back to the editor instead of wrapping around to the last pill, and the selected pill renders with a highlighted background instead of blending into the dim `·`-joined footer text. Regressions in `test/interactive-mode-footer-nav.test.ts`.

- System prompt: extracted the guideline strings shared between the default prompt and Bash `promptGuidelines` into `core/prompt-guidelines.ts` constants. The hand-copied Bash variant had drifted ("Prefer native file tools…" vs "File/dir exploration uses native tools…"), defeating `addGuideline`'s exact-string dedupe — sessions carried both near-duplicate bullets. Prompts now collapse to the single canonical rule; regression in `test/system-prompt.test.ts` asserts byte-identity and single occurrence.

- Agents runs selector: pressing Enter on a running agent row now enters the zoomed session view for that row (row-scoped zoom entry); non-running rows keep the existing detail view. Regression in `test/interactive-mode-agents-command.test.ts`.

- System prompt: tool-provided `promptGuidelines` are no longer silently dropped when a custom system prompt is set — they render as a `Tool guidelines:` section before the dynamic boundary (cached prefix; byte-identical output when no guidelines are provided). Regressions in `test/system-prompt.test.ts`.

- Bash: enforce the long-documented native-tool guard at runtime — commands whose first pipeline stage is standalone `grep`/`egrep`/`fgrep`/`rg`/`find`/`ls` (including behind env assignments, wrappers like `sudo`/`env`/`nice`, subshell parens, and absolute paths) are rejected with steering to the native Grep/Glob/Ls tools; pipeline filters on command output (`kubectl ... | grep Ready`) remain allowed and heredoc bodies are ignored. Grep tool description gains Claude-Code-parity ripgrep steering (ALWAYS Grep / NEVER bash grep, brace escaping, outputMode, multiline), the explore agent prompt no longer suggests bash `grep` pipelines for repo files, and Bash promptGuidelines wording now matches the enforced behavior. Regressions in `test/bash-native-tool-guard.test.ts`.

- Suppress only the known benign pi-goal stale queued-continuation abort (`pi-goal:stale-queued-continuation-cancelled`) in interactive rendering while preserving visible text for normal aborts and errors.

- Footer: while an auto-model alias is pending, show only the alias (e.g. `openai-codex/auto`) instead of `alias → seed-model`. The concrete model behind a pending alias is just the unrouted compat seed, so the old arrow display read as if routing had already resolved (to the seed, typically `gpt-5.3-codex-spark`); the alias clears on resolve, so the routed model appears as soon as it exists. Regression in `test/footer-width.test.ts`.

- Route settings-persisted auto model defaults (`defaultProvider` + `defaultModel: "auto"`) through the same deferred `model:resolve` pending-auto request as `--model auto`, so a persisted `openai-codex/auto` default semantically routes at the first prompt boundary instead of silently landing on `findInitialModel`'s concrete fallback model. Also fixed for configs with `enabledModels` (scoped models): `buildSessionOptions`' scoped-model fallback previously discarded the auto intent before the SDK seam could see it, and auto placeholder seeding now prefers in-scope models over the full registry. Concrete defaults, explicit `--model`, and session restore are unaffected. Regressions in `test/sdk-settings-default-auto.test.ts` and `test/main-session-options-settings-auto.test.ts`.

- Keep the `/resume` / `--resume` session picker default on the Named filter, but treat sessions with a non-placeholder first user message as displayable named results so the default hides only empty stubs instead of normal unnamed sessions.

- Fix a fatal race in `AgentSession.prompt()`: the busy gate now checks `isCompacting`/`agent.isProcessing` in addition to `isStreaming` (mirroring `sendCustomMessage`), so prompts and extension `sendUserMessage` calls sent during auto-compaction, the post-compaction resume, or the agent_end listener phase are queued (default steer) instead of racing `agent.prompt()` and rejecting with "Agent is already processing a prompt" — an un-awaited rejection there crashed the whole interactive process mid-"Auto-compacting…". A TOCTOU fallback at the `_runAgentPrompt` send chokepoint routes raced prompt messages into the steer/follow-up queue, and manual `compact()` now drains messages queued while it held the busy gate (it aborts any active run, so nothing else would deliver them). Regressions in `test/agent-session-concurrent.test.ts`.
- Fix `fast`/`medium` child-agent model aliases for `clawrouter`, warn when `fast` falls back to a parent/frontier model, and clamp unsupported `thinking: "off"` to the lowest supported model thinking level.

- Add an experimental Pierre-backed Edit TUI diff renderer behind `PI_TUI_DIFF_RENDERER=pierre` / `PI_DIFF_RENDERER=pierre`, covering both structured hunk rendering and the legacy no-hunks fallback while keeping the existing fast terminal renderer as the default.
- Skip npm audit during managed package installs by passing `--no-audit` alongside `--legacy-peer-deps`, avoiding Arborist audit/rollback failures during extension refreshes while leaving bun/pnpm install behavior unchanged.
- Add `ctx.requestStopAfterTurn(reason?)` for extensions that need to park the current agent run at the next turn boundary without aborting and without changing mixed tool-batch `terminate` semantics. Covers mixed terminating/non-terminating tool batches with `test/suite/agent-session-extension-stop-after-turn.test.ts`.
- Fix `requestStopAfterTurn()` silently dropping the stop request when called from inside a tool's own `execute()` (e.g. pi-goal's `goal_wait`) while `AgentSession.isStreaming` briefly reads false during a still-active run (compaction/agent_end-phase race, same window as `agent-session-concurrent.test.ts:307`). The setter now always latches `_extensionStopAfterTurnReason`, matching the fact that it is already reset on every `agent_start`/`agent_end`; latching outside a run is a no-op by construction. Regression added to `test/suite/agent-session-extension-stop-after-turn.test.ts` (fails on the `isStreaming` guard, passes with the unconditional latch).
- Add `ForkAgentOptions.detachFromParent` (additive, default-off): a fork can opt out of the parent session's abort signal so a `AgentSession.dispose()`/`agent.abort()` on session replace/reload no longer cancels a best-effort background fork; the fork then runs on the caller's own signal, bounded by the caller's drain. Consumed by pi-memory turn-end extraction (drained on `session_shutdown`). Lifecycle flag only, not part of the cached prefix. Locks the drain-before-abort invariant with a regression test in `test/agent-session-runtime-events.test.ts`. Refs #151/#152.
- Add cache-safe semantic auto model routing boundaries: `model:resolve` hooks can resolve `auto` / provider-scoped `*/auto` aliases at session, child-agent, or first-prompt interactive boundaries; unresolved aliases degrade gracefully (visible routing warning, current model and cache affinity untouched) or remain pending instead of silently using a seed model, and parallel Agent child rows expose routed child task details.
- Register `clawrouter` as an auto-alias provider (`clawrouter/auto` in the model selector and `AUTO_MODEL_ALIAS_PROVIDERS`), and degrade unresolved auto aliases at the prompt/startup boundary with a `model-routing-warning` custom message instead of throwing `did not resolve to a semantic routing decision`.
- Rename the visible semantic auto alias from `pi-fork/auto` to `clawrouter/auto`: CLI/settings/model-picker/child-agent inputs canonicalize old `pi-fork/auto` spelling to `clawrouter/auto`, and first-prompt auto routing now emits model/UI events even when the selected concrete model matches the startup seed so the footer clears the pending alias immediately.
- Add a first-class Glob `ignore:false` retry mode for ignored known paths while keeping ignore-aware defaults and VCS metadata exclusions.
- Show running background bash jobs in the existing background status footer/pane by backing it with the unified task registry instead of agent-only status.
- Flag likely prompt-cache TTL expiry as cache-health telemetry in session logs and the interactive footer so cold turns after warm same-model turns are easier to diagnose.
- Bound Grep's formatted model-facing output before the final join so broad or `full:true` searches cannot exhaust memory before truncation; suppress noisy skill collision warnings for byte-identical duplicate installs.
- Cap aggregate model-facing tool-result text at 100k chars after extension `tool_result` hooks, preserve full text artifacts under `.pi/tool-results`, and bound edit-tool original-content details to `originalContentPreview`.
- Fix cache-safe split-turn compaction summaries so the `Turn Context (split turn)` section uses a delta-only format instead of repeating the main Goal/Progress/Next Steps checkpoint headings.
- macOS: route search backends (`rg`/`fd`/`ugrep`/`bfs`) exclusively through Pi's managed `~/.pi/bin` instead of system-PATH (Homebrew) binaries. Homebrew's adhoc-signed binaries trigger a Gatekeeper assessment (`amfid`/`syspolicyd`/`trustd`) on every spawn; under Pi's hot Grep/Find loops this pinned the CPU. Managed copies carry no quarantine xattr and are assessment-inert; `downloadTool` strips the xattr as belt-and-suspenders. Offline mode keeps the PATH fallback (for both required and optional backends) so a machine with no managed copy can still search. darwin-gated; no effect on Linux/Termux/Windows.
- Reap stale `<session>.live` liveness markers proactively: add `sweepStaleMarkers()` and a deferred, unref'd sweep on interactive startup so markers left by crashed/SIGKILL'd sessions can no longer accumulate without bound (354 dead markers observed on one machine). Only dead/stale markers are removed; live sessions are untouched.
- Add opt-in resident session pruning after successful compaction and during compacted-session hydration (`compaction.residentPrune` / `PI_RESIDENT_SESSION_PRUNE=1`), planning current-version loads from raw-line metadata and applying stubs before summarized candidate payload JSON is parsed, plus a Claude/Pi RSS audit harness.
- Report loaded context usage separately from unloaded provider-deferred tool schemas in context-usage diagnostics and the TUI footer.
- Expose `ctx.reload()` on extension event contexts so model-switch handlers can rebuild provider-specific runtime resources without queuing slash-command text.
- Agents status pane now shows a live elapsed-seconds counter for in-progress runs (e.g. `running 12s`): `formatDuration` renders live elapsed for `running` runs, and `AgentsPane` repaints on a 1s ticker (duplicate-guarded, `unref`'d, cleared on dispose). Previously running rows showed a static `running` placeholder.
- Remove the dormant, never-activated background-tasks pane (`background-tasks-ui.ts`). It was added but never imported into the runtime and is superseded by the agents status pane; the public `Task*` tool factories in `core/tools/background-tasks.ts` are unchanged.
- Perf: `main.ts` dispatch now lazy-imports `InteractiveMode`, `runRpcMode`, export-html, session-picker, and startup-ui at their mode/flag branches, so headless invocations (`--help`, `-p`, `--mode json`/`rpc`) never parse interactive-mode. `runPrintMode` is imported directly from `modes/print-mode.ts` instead of the modes barrel. Behavior, error messages, and `--help` ordering unchanged.
- Add per-extension boot timings to `PI_TIMING=1`: ranked report of each eager extension `import()` duration and each `session_start` handler duration (`timings.ts` `recordTiming`, wired in extension loader/runner). Zero overhead when unset.

This file is fork-owned (`.gitattributes` `merge=ours`) so upstream syncs no
longer append their full changelog here.
