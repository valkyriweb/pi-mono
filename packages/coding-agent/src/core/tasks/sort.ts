/**
 * Task sorting helpers moved to the pi-agent-ui extension package.
 *
 * This module intentionally exports nothing; it remains as a tombstone so the
 * source extraction is explicit without deleting a core file in this change.
 */
